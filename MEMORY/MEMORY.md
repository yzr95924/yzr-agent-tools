- [Test isolation invariants](test-isolation-invariants.md) — why tests must NEVER touch the real agent config (e.g. `~/.claude/settings.json`), and the enforcement mechanism in `tests/conftest.py`.
- [qodercli driver not feasible](qodercli-driver-not-feasible.md) — verified 2026-07-31 (model-switch only): cloud-relay-only + server-side BYOK gate; do NOT build a qodercli **model-switch** driver unless qoder ships direct-baseURL. NOTE: qodercli **MCP** is a separate, feasible, client-side feature (implemented as an mcp-plugin-mgr driver) — don't conflate the two.
